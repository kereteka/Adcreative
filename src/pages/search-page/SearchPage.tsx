import Search from '../../feature/search/Search';

type Props = {};

const SearchPage = (props: Props) => {
  return (
    <div>
      <Search />
    </div>
  );
};

export default SearchPage;
